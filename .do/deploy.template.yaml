spec:
  name: timraphael-com
  static_sites:
    - build_command: bundle exec jekyll build -d ./public
    environment_slug: jekyll
    git:
      branch: main
      repo_clone_url: https://github.com/tardoe/timraphael-com.git
    name: timraphael-com
